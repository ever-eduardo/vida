
var bin = load("std/binary")
assert(bin)

var time = load("std/time")
assert(time)

var cast = load("std/cast")
assert(cast)

var exception = load("std/exception")
assert(exception)

var corelib = load("std/corelib")
assert(corelib)
